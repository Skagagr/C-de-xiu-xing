#include <stdio.h>
#define FEN 60  //1分钟=60秒

int main(void) 
{
    int miao,fen;

    printf("输入秒转换为分钟：");
    scanf_s("%d", &miao);
    fen = miao / FEN;   //截断分钟数 
    miao = miao % FEN;  //剩余秒数
    printf("%d分钟%d秒", fen, miao);    // 求整/ 和 求余% 运算符

    return 0;
}
